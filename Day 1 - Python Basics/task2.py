#Write a program that calculates the area of a rectangle using user-provided length and width

length = float(input("What is the length of the rectangle(cm): "))
width = float(input("What is the width of the rectangle(cm): "))

area = length * width

print(f"The area of the rectangle is {area}cm")